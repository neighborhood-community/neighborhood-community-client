import { styled } from 'styled-components';

const M = {
  TextAreaWrapper: styled.div`
    width: inherit;
    height: 31.25rem;
  `,
  TextArea: styled.textarea`
    width: inherit;
    height: inherit;
  `,
};

export { M };
